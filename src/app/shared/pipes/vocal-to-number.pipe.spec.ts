import { VocalToNumberPipe } from './vocal-to-number.pipe';

describe('VocalToNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new VocalToNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
